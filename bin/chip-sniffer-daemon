#!/bin/sh
set -eu

IFACE="${1:-usb0}"
OUTDIR="/var/lib/chip-sniffer/captures"

mkdir -p "$OUTDIR"
chmod 0755 "$OUTDIR"

TS="$(date +%Y%m%d-%H%M%S)"
BASE="$OUTDIR/${IFACE}-${TS}.pcap"

exec /usr/sbin/tcpdump -i "$IFACE" -n -s 0 \
  -C 5 -W 6 \
  -w "$BASE"
